1.	String s = “abcd”,从中取出任意元素后剩余字符串表示
C++
For(int i=0;i<s.size();i++)
{
	String element = s.substr(i,1);
	String left = s.substr(0,i);
	String right = s.substr(i+1,s.size()-1-i);
}
Java
For(int i=0;i<s.length();i++)
{
	String element = s.substring(i,i+1);
	String left = s.substring(0,i);
	String right = s.substring(i+1,s.length());
}

2.given a vector of integers, v[i] represent the stock price on day i. Now you may do at most K transactions. you must sell your stock before you buy it again and that means you can NOT have two stocks at the same time. write a program to find max profit you can get.
