1 如何判断两个机器属于同一个子网络？
看子网掩码，子网掩码是用来决定那一部分为网络部分，哪一部分为主机部分。网络部分用1，主机部分用0.
比如一个子网掩码为255.255.255.192转换成2进制就是11111111.11111111.11111111.11000000。说明前26位均为网络部分，如果两个网址前26位相同，则属于同一个子网

2 什么场景下需要判断两个机器是否属于同一个子网络？
广播的时候，即一对多发送数据时，将网络划分成子网，避免网络风暴

3 一个数组，给出一个数，找出数组中哪两个元素的和等于该数。
思路一：Brute foce，遍历每一种可能，复杂度O（n2）
思路二：先排序，再对每个元素使用二分查找（查找sum-first element），复杂度O（n2）
思路三：先排序，维护两个指针，一个从前到后，一个从后到前，当和>sum时，first++，反之last--，复杂度O（n2）
思路四：把数组存入hash表，对每个元素查找sum-first element，复杂度O（n），空间复杂度O（n）

4 给出一个词典，找出词典里所有的变位词（anagram），分析时间复杂度。
Brute force, 对于每个词，与之后每个词比较，比较的时候判断二者是否含有相同的字母，时间复杂度O（n2m）
优化，比较之前判断每个词的长度，只与长度相同的词比较，时间复杂度不变
继续优化，对每个词按照字母进行排序，然后再对整个词典进行排序，排序之后所有变位词就前后相连，复杂度O（mlogm*n + n*logn）

5 堆和栈的区别。
堆内存用来存放程序运行时产生的动态数据，一般通过关键字new创建，需要手动释放
栈内存用来存放程序编译时产生的静态数据，一般存放局部变量、函数参数等，会在函数结束后自动释放

6 大量的new会导致什么问题？
可能会导致内存溢出

7 堆内存如何管理内存？
用reference counting

8 给出两个文件，每个文件50亿URL，每个URL64个字节。找出两个文件中共同的URL。
方案一
每个文件5*64 = 320G，不可能放入内存，故需要将文件分割成小文件
对a文件进行hash(a)/1000，则50亿URL会进行1000个不同的文件，得到a0,a1,...a999
对b文件进行同样地操作，得到b0,b1,...b999
相同的URL一定在对应的文件中，不同的文件一定包含不同的URL，如a0跟b999一定没有相同的URL
小文件每个大小在300M左右，可以放进内存操作，用hashmap，进行遍历即可
方案二
如果允许出错，则可以使用bloom filter将一个文件映射到bloom filter中，4G内存大概可以表示40亿*8 = 320亿bit的数据
映射完之后遍历第二个文件，查询每个文件是否存在

9 bloom filter的原理及意义
在Bloom filter之前，查询数据是否出现过常见的方案有：
（1）在数据库中查询
（2）用hashset或hashtable进行查询
（3）数据经过MD5之类的加密方法之后再放入hashset
（4）使用Bit-map方法，将每个数据经过哈希函数映射到某一位
但是当数据量特别大时，上述方法也存在问题
（1）关系数据库查询，速度太慢
（2）太消耗内存，1亿条URL，每条50字节，就需要5G
（3）经过MD5之后每天URL能够下降到128bit=16字节，节省几倍内存，但仍然太大
（4）同样地数据，用1亿*1/8=0.0125G内存就够了，但是问题在与错误率太高。要降低冲突发生的概率到1%，就要将BitSet的长度设置为URL个数的100倍
bloom filter采取与bitset类似的思路，但通过多个hash的使用，将错误率大大降低，同时对内存的消耗也在接受范围之内。因此是大数据处理的常用工具
研究显示，当bloom filter中hash函数个数为10，位数组大小m为数据个数20倍（1亿条数据，需要1亿*20/8=0.3G）时，冲突概率在0.0000889左右。
http://www.cnblogs.com/heaad/archive/2011/01/02/1924195.html
